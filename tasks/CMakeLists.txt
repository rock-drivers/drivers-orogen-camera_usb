# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(orogen_camera_usbTaskLib)
ADD_LIBRARY(${OROGEN_CAMERA_USB_TASKLIB_NAME} SHARED 
    ${OROGEN_CAMERA_USB_TASKLIB_SOURCES})

add_dependencies(${OROGEN_CAMERA_USB_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${OROGEN_CAMERA_USB_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${OROGEN_CAMERA_USB_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${OROGEN_CAMERA_USB_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${OROGEN_CAMERA_USB_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${OROGEN_CAMERA_USB_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${OROGEN_CAMERA_USB_TASKLIB_HEADERS}
    DESTINATION include/orocos/orogen_camera_usb)

