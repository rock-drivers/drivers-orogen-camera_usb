# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(camera_usbTaskLib)
ADD_LIBRARY(${CAMERA_USB_TASKLIB_NAME} SHARED 
    ${CAMERA_USB_TASKLIB_SOURCES})

add_dependencies(${CAMERA_USB_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${CAMERA_USB_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${CAMERA_USB_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CAMERA_USB_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CAMERA_USB_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CAMERA_USB_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CAMERA_USB_TASKLIB_HEADERS}
    DESTINATION include/orocos/camera_usb)

